{
  "install": "curl -fsSL https://install.pears.com/pear.sh | sh && export PATH=\"$HOME/.pear/bin:$PATH\" && npm install",
  "terminals": [
    {
      "name": "dev",
      "command": "npm start"
    }
  ]
}
