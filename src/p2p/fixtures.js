'use strict'

const { FIXTURE_TOPIC_HEX } = require('./topic')

/** Frozen contract fixtures — mirror in src/contracts/fixtures.js when Agent-A lands it. */
module.exports = {
  TOPIC_HEX: FIXTURE_TOPIC_HEX,
  PAYLOAD_UTF8: 'hello jojun'
}
